var personas = prompt("¿Cuántas personas van a viajar?")
var distanciaKM = prompt("¿Cuántos kilómetros se recorrerán?")
var tarifaelegida = prompt("¿Qué tipo de camión requieres?")
//el input en teoría tiene que corresponder
//con la variable que tenga asignado el costo de la tarifa (valor numérico).
//So: A OR B OR C

var C = 2.0 // por kilómetro por persona
var B = 2.5 // por kilómetro por persona
var A = 3.0 // por kilómetro por persona

if ( tarifalegida = C ){
	costo = 2.0
	if (personas>=20) {

	};
	if (personas<=19) {
		costototal = 20 * costopp
	};
	var costopp = C * distanciaKM
	var costototal = personas * costopp
	alert("El precio del viaje por persona es de $" + costopp + " pesos")
	alert("El precio total del viaje si asisten " + personas + " personas es de $" + costototal)
}